---

slug: /cos
---

# COS

## Declaration

```sql
COS(X)
```

## Description

Returns the cosine of the given radians `X`.

## Example

```sql
SELECT COS(PI());
+-----------+
| COS(PI()) |
+-----------+
|        -1 |
+-----------+
1 row in set (0.001 sec)
```